export enum TicTacToeStatus {
  NOT_STARTED = 'not_started',
  IN_PROGRESS = 'in_progress',
  WIN = 'win',
  LOSE = 'lose',
  TIE = 'tie',
}

export enum TicTacToeMultiplier {
  LOW = 'easy',
  MEDIUM = 'medium',
  HIGH = 'hard',
}

export enum XOTurn {
  X = 'X',
  O = 'O',
}
