# ================================
# ENVIRONMENT SETTING
# ================================
# For development: NODE_ENV=development (or just comment out for default)
# For production: NODE_ENV=production
NODE_ENV=development
APP_URL=https://staging.blockwinz.com

# ================================
# APPLICATION SETTINGS
# ================================
APP_CONTAINER_NAME=blockwinz-api
PORT=7979
BULLBOARD_PORT=8080
# Set to true only for explicitly protected admin environments.
ENABLE_BULL_BOARD=false
# BULL_BOARD_USERNAME=
# BULL_BOARD_PASSWORD=
CLUSTER_WORKER_NUMBER=0

# ================================
# DATABASE (PostgreSQL – Supabase or other host)
# ================================
# Use the connection string from Supabase: Project Settings → Database.
# Prefer the pooled URI (transaction mode, port 6543) for the app; use direct (5432) for migrations if needed.
# Example:
# DATABASE_URL=postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?sslmode=require
DATABASE_URL=
#
# Optional: only if DATABASE_URL is not set (e.g. local Postgres)
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=
# POSTGRES_HOST=localhost
# DATABASE_PORT=5432
# POSTGRES_DB=blockwinz

# ================================
# REDIS SETTINGS (one hosted instance for local + Railway)
# ================================
# The API reads only REDIS_URL (see redisAdapter / app.module).
#
# Local dev (laptop): use Railway Redis over the public TCP proxy — same data as prod.
#   Railway → Redis service → Variables → copy the resolved REDIS_PUBLIC_URL
#   (redis://default:<password>@<RAILWAY_TCP_PROXY_DOMAIN>:<RAILWAY_TCP_PROXY_PORT>)
#
# API service on Railway: use the Redis plugin’s private URL (same DB, private network).
#   On the API service, set REDIS_URL from the Redis service (Railway “Variable Reference” to that service’s REDIS_URL).
#   or paste the private redis://default:...@<RAILWAY_PRIVATE_DOMAIN>:6379 value.
#
# Optional: redis://localhost:6379 only if REDIS_URL is unset and you run Redis in Docker.
REDIS_URL=redis://default:replace-me@localhost:6379
REDIS_CONTAINER_NAME=redis
REDIS_PORT=6379

# ================================
# SECURITY & AUTHENTICATION
# ================================
JWT_SECRET=replace-with-strong-random-secret
# Used only when you run `pnpm seed` (not on app startup)
DEFAULT_ADMIN_EMAIL=ceo@blockwinz.com

# Used for encrypting/decrypting user wallet private keys
# This is a random string of characters
DATA_ENCRYPTION_KEY=replace-with-separate-strong-encryption-key

# Optional PostHog Cloud analytics / feature flags
# POSTHOG_ENABLED=false
# POSTHOG_API_KEY=phc_your_project_api_key
# POSTHOG_HOST=https://us.i.posthog.com

# ================================
# EMAIL (Mailgun)
# ================================
# API key: Mailgun Dashboard → Sending → Domain settings → API keys
MAILGUN_API_KEY=
# Sending domain (e.g. mg.yourdomain.com)
MAILGUN_DOMAIN=
# From header; must be an address on the domain above
MAILGUN_FROM=Blockwinz <noreply@mg.yourdomain.com>
# Optional: EU region — https://api.eu.mailgun.net (default US: https://api.mailgun.net)
# MAILGUN_API_URL=https://api.eu.mailgun.net

# ================================
# BLOCKCHAIN SETTINGS - NEO
# ================================
NEO_ENV=TestNet
# MainNet or TestNet
NEO_BLOCKWINZ_PRIVATE_KEY=replace-with-neo-private-key
NEO_BLOCKWINZ_ADDRESS=replace-with-neo-address

# ================================
# BLOCKCHAIN SETTINGS - SOLANA
# ================================
SOLANA_NETWORK=testnet
# Set to true only when an authenticated admin should be allowed to use /wallet/send-bwz on testnet.
ENABLE_TESTNET_BWZ_FAUCET=false
# Options: 'devnet' | 'testnet' | 'mainnet-beta'
SOLANA_BLOCKWINZ_MINT_ADDRESS=replace-with-solana-mint-address
SOLANA_BLOCKWINZ_PRIVATE_KEY=replace-with-solana-private-key
SOLANA_BLOCKWINZ_ADDRESS=replace-with-solana-address

# ================================
# DOCKER COMPOSE SETTINGS
# ================================
# For development: Mount source code with read-write access
# For production: No mounting (will be ignored anyway)
MOUNT_TYPE=rw



# ============================
# COIN GECKO 
# ===========================
COIN_GECKO_API_KEY=replace-with-coingecko-api-key

# ================================
# MULTIPLAYER (optional)
# ================================
# Max ms a vacant pending lobby (empty players[]) may sit before auto-cancel; lobbies with anyone seated are not expired by this timer (fallback if plugin lobbyWaitMs unused)
# MULTIPLAYER_LOBBY_MAX_WAIT_MS=600000
# Rake in basis points (100 = 1%) on total pot; requires MULTIPLAYER_RAKE_RECEIVER_USER_ID
# MULTIPLAYER_RAKE_BPS=0
# MULTIPLAYER_RAKE_RECEIVER_USER_ID=
# Quoridor: per-turn server deadline in ms (5_000–600_000; default 25_000). On expiry a legal move is applied automatically.
# QUORIDOR_TURN_MS_MS=25000