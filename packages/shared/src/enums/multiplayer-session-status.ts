/** Multiplayer `game_sessions.game_status` values (lobby + play lifecycle). */
export enum MultiplayerSessionStatus {
  PENDING = 'pending',
  IN_PROGRESS = 'in_progress',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled',
}
