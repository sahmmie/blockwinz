import { defineTokens } from "@chakra-ui/react";

const tokens = defineTokens({
    colors: {
        brand: {
            default: {
                value: '#000A27',
            },
        },
        alt: {
            default: {
                value: '#151832',
            },
        }
    },
});

export default tokens;